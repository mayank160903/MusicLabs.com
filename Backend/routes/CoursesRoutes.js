const express = require("express");
const router = express.Router();

// router.route("/:chatId").get(auth,allMessages);

// router.route("/").post(sendMessage);
router.post("/catalogue");
router.post("/purchase/:coursename");
router.



module.exports = router;
